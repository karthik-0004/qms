FROM python:3.12-slim AS builder
WORKDIR /build
COPY pyproject.toml .
RUN pip install --no-cache-dir setuptools wheel && pip install --no-cache-dir .

FROM python:3.12-slim AS runtime
RUN groupadd -r appuser && useradd -r -g appuser appuser
WORKDIR /app
COPY --from=builder /usr/local/lib/python3.12/site-packages /usr/local/lib/python3.12/site-packages
COPY --from=builder /usr/local/bin /usr/local/bin
COPY app/ ./app/
COPY alembic.ini ./alembic.ini
COPY migrations/ ./migrations/
RUN chown -R appuser:appuser /app
USER appuser
EXPOSE 8044
HEALTHCHECK --interval=30s --timeout=5s --start-period=10s --retries=3 \
    CMD python -c "import urllib.request; urllib.request.urlopen('http://localhost:8044/health')"
CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8044", "--workers", "1"]
